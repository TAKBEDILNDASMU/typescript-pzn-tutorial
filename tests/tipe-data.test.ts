describe("Data Type", () => {
  it("should be able to declare", () => {
    const name: string = "Patrick Star";
    const balance: number = 100000;
    const isVip: boolean = true;

    console.info(name);
    console.info(balance);
    console.info(isVip);
  });
});
